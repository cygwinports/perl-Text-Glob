perl-Text-Glob
------------------------------------------
Text::Glob implements glob(3) style matching that can be used to match 
against text, rather than fetching names from a filesystem. If you want 
to do full file globbing use the File::Glob module instead.

Runtime requirements:
  perl-5.10.0-5

Build requirements:
  cygport-0.9.9-1
  make-3.81-2
  perl-5.10.0-5

Canonical website:
  http://search.cpan.org/dist/Text-Glob/

Canonical download:
  http://search.cpan.org/CPAN/authors/id/R/RC/RCLAMP/

-------------------------------------------

Build instructions:
  unpack perl-Text-Glob-0.08-X-src.tar.bz2
    if you use setup to install this src package, it will be
	 unpacked under /usr/src automatically
  cd /usr/src
  cygport ./perl-Text-Glob-0.08-X.cygport all

This will create:
  /usr/src/perl-Text-Glob-0.08-X-src.tar.bz2
  /usr/src/perl-Text-Glob-0.08-X.tar.bz2

-------------------------------------------

Port Notes:

----- version 0.08-2 -----
Initial release for Cygwin 1.7.


Cygwin port maintained by:
http://sourceware.org/cygwinports/
